{
  "name": "betterspec-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "turbo": "^2.9.4",
    "typescript": "^6.0.2",
    "tsup": "^8.5.1",
    "vitest": "^4.1.2",
    "@types/node": "^25.5.2",
    "prettier": "^3.8.1"
  },
  "overrides": {
    "path-to-regexp": ">=8.4.0",
    "picomatch": ">=4.0.4",
    "vite": ">=7.3.2"
  }
}
